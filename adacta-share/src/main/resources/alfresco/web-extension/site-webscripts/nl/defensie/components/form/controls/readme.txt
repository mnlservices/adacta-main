The reason that some control fields are copied have to do with the tab mechanism during indexing. 
Using the tab will also focus on the ? which is a extra 'tab'. The reason why Alfresco use the 
tabindex on the ? is not clear.  